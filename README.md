# Communication-Script-
A software that lets you send encrypted messages to someone else running the code using IPs that can't be blocked by the network admins (you can block it on your end), and it only uses the network, not the internet.
